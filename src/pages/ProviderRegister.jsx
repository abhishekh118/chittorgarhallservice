import { useState } from "react"; import { Link,useNavigate } from "react-router-dom"; import API from "../api"; import {useAuth} from "../context/AuthContext"; import { BriefcaseBusiness } from "lucide-react";
export default function ProviderRegister(){const[form,setForm]=useState({name:'',email:'',phone:'',password:'',role:'provider',location:{address:'',area:'',city:'Chittorgarh',pincode:''}}),[error,setError]=useState('');const{login}=useAuth();const nav=useNavigate();const loc=(k,v)=>setForm({...form,location:{...form.location,[k]:v}});const submit=async e=>{e.preventDefault();setError('');try{const{data}=await API.post('/auth/register',form);login(data);nav('/provider-onboarding')}catch(err){setError(err.response?.data?.message||'Registration failed')}};return <section className="register-page"><div className="register-intro provider"><div className="auth-icon provider"><BriefcaseBusiness/></div><span>Service provider account</span><h1>Take your business online</h1><p>Register separately as a professional or business and complete verification before receiving bookings.</p><ul><li>Create business profile</li><li>Upload KYC securely</li><li>Receive paid bookings</li></ul></div><form className="register-card" onSubmit={submit}><h2>Register service provider</h2>{error&&<div className="form-error">{error}</div>}<div className="form-grid"><div><label>Full name</label><input required value={form.name} onChange={e=>setForm({...form,name:e.target.value})}/></div><div><label>Mobile number</label><input required maxLength="10" value={form.phone} onChange={e=>setForm({...form,phone:e.target.value})}/></div><div><label>Email address</label><input type="email" required value={form.email} onChange={e=>setForm({...form,email:e.target.value})}/></div><div><label>Password</label><input type="password" minLength="8" required value={form.password} onChange={e=>setForm({...form,password:e.target.value})}/></div><div><label>Area</label><input value={form.location.area} onChange={e=>loc('area',e.target.value)}/></div><div><label>Pincode</label><input value={form.location.pincode} onChange={e=>loc('pincode',e.target.value)}/></div><div className="span-2"><label>Full address</label><input value={form.location.address} onChange={e=>loc('address',e.target.value)}/></div></div><label className="consent-row"><input type="checkbox" required/> I agree to Terms, Privacy Policy and platform rules.</label><button className="btn dark-sky full">Register as Provider</button><p className="form-foot">Already have a provider account? <Link to="/provider/login">Login here</Link></p></form></section>}
