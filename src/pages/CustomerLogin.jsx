import { useState } from "react"; import { Link,useNavigate } from "react-router-dom"; import { ShieldCheck,UserRound } from "lucide-react"; import API from "../api"; import {useAuth} from "../context/AuthContext";
import "./Auth.css";
import "./CustomerLogin.css";
export default function CustomerLogin(){const[form,setForm]=useState({emailOrPhone:'',password:'',expectedRole:'customer'}),[error,setError]=useState('');const{login}=useAuth();const nav=useNavigate();const submit=async e=>{e.preventDefault();setError('');try{const{data}=await API.post('/auth/login',form);login(data);nav('/dashboard')}catch(err){setError(err.response?.data?.message||'Login failed')}};return <AuthLayout type="customer"><form className="new-auth-card" onSubmit={submit}><div className="auth-icon customer"><UserRound/></div><span>Customer access</span><h1>Welcome back</h1><p>Apni bookings, payments aur reviews manage karein.</p>{error&&<div className="form-error">{error}</div>}<label>Email or mobile number</label><input required value={form.emailOrPhone} onChange={e=>setForm({...form,emailOrPhone:e.target.value})}/><label>Password</label><input type="password" required value={form.password} onChange={e=>setForm({...form,password:e.target.value})}/><button className="btn sky full">Customer Login</button><div className="auth-safe"><ShieldCheck/> Secure account access</div><p className="form-foot">New customer? <Link to="/customer/register">Create account</Link></p><p className="switch-login">Are you a provider? <Link to="/provider/login">Provider login</Link></p></form></AuthLayout>}
function AuthLayout({children}){return <section className="new-auth-section"><div className="auth-art"><div><span>Chittorgarh All Services</span><h2>Local services, safely connected.</h2><p>Verified providers, transparent charges and simpler bookings.</p></div></div>{children}</section>}
