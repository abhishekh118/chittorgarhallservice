import { useState } from "react"; import { Link,useNavigate } from "react-router-dom"; import { BadgeCheck,BriefcaseBusiness } from "lucide-react"; import API from "../api"; import {useAuth} from "../context/AuthContext";
export default function ProviderLogin(){const[form,setForm]=useState({emailOrPhone:'',password:'',expectedRole:'provider'}),[error,setError]=useState('');const{login}=useAuth();const nav=useNavigate();const submit=async e=>{e.preventDefault();setError('');try{const{data}=await API.post('/auth/login',form);login(data);nav('/dashboard')}catch(err){setError(err.response?.data?.message||'Login failed')}};return <section className="new-auth-section provider-auth"><div className="auth-art provider"><div><span>Business portal</span><h2>Grow your service business locally.</h2><p>Complete KYC, list services and manage verified customer bookings.</p></div></div><form className="new-auth-card" onSubmit={submit}><div className="auth-icon provider"><BriefcaseBusiness/></div><span>Provider access</span><h1>Business login</h1><p>Provider dashboard aur KYC status access karein.</p>{error&&<div className="form-error">{error}</div>}<label>Email or mobile number</label><input required value={form.emailOrPhone} onChange={e=>setForm({...form,emailOrPhone:e.target.value})}/><label>Password</label><input type="password" required value={form.password} onChange={e=>setForm({...form,password:e.target.value})}/><button className="btn dark-sky full">Provider Login</button><div className="auth-safe"><BadgeCheck/> Verified business portal</div><p className="form-foot">New provider? <Link to="/provider/register">Register business</Link></p><p className="switch-login">Need a service? <Link to="/customer/login">Customer login</Link></p></form></section>}
