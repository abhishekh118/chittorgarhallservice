import { useState } from "react"; import { Link,useNavigate } from "react-router-dom"; import API from "../api"; import {useAuth} from "../context/AuthContext"; import { UserRound } from "lucide-react";
export default function CustomerRegister(){const[form,setForm]=useState({name:'',email:'',phone:'',password:'',role:'customer',location:{address:'',area:'',city:'Chittorgarh',pincode:''}}),[error,setError]=useState('');const{login}=useAuth();const nav=useNavigate();const loc=(k,v)=>setForm({...form,location:{...form.location,[k]:v}});const submit=async e=>{e.preventDefault();setError('');try{const{data}=await API.post('/auth/register',form);login(data);nav('/dashboard')}catch(err){setError(err.response?.data?.message||'Registration failed')}};return <section className="register-page"><div className="register-intro customer"><div className="auth-icon customer"><UserRound/></div><span>Customer account</span><h1>Book trusted local services</h1><p>Create your personal account for faster bookings, secure payments and service history.</p><ul><li>Compare verified providers</li><li>Online payment breakdown</li><li>Booking and review history</li></ul></div><form className="register-card" onSubmit={submit}><h2>Create customer account</h2>{error&&<div className="form-error">{error}</div>}<div className="form-grid"><div><label>Full name</label><input required value={form.name} onChange={e=>setForm({...form,name:e.target.value})}/></div><div><label>Mobile number</label><input required maxLength="10" value={form.phone} onChange={e=>setForm({...form,phone:e.target.value})}/></div><div><label>Email address</label><input type="email" required value={form.email} onChange={e=>setForm({...form,email:e.target.value})}/></div><div><label>Password</label><input type="password" minLength="8" required value={form.password} onChange={e=>setForm({...form,password:e.target.value})}/></div><div><label>Area</label><input value={form.location.area} onChange={e=>loc('area',e.target.value)}/></div><div><label>Pincode</label><input value={form.location.pincode} onChange={e=>loc('pincode',e.target.value)}/></div><div className="span-2"><label>Full address</label><input value={form.location.address} onChange={e=>loc('address',e.target.value)}/></div></div><label className="consent-row"><input type="checkbox" required/> I agree to Terms, Privacy Policy and platform rules.</label><button className="btn sky full">Create Customer Account</button><p className="form-foot">Already registered? <Link to="/customer/login">Login here</Link></p></form></section>}
